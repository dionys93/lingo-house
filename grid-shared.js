// web/src/components/house/grid-shared.js
// One shared constant, in its own file so the engine and the public shelf
// can both import it without a circular dependency.
export const EXTERIOR = 'exterior';